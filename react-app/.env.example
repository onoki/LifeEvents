# Optional full URL of the allowlisted Vercel index endpoint.
# Leave unset to use only the existing direct/public-proxy implementation.
VITE_INDEX_API_URL=https://life-events-five.vercel.app/api/index-history
